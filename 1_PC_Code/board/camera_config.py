"""
摄像头模块 — 可调参数集中管理
所有配置项一处修改，无需改动业务代码
"""

# ============================================================
# 摄像头硬件参数
# ============================================================
DEVICE = "/dev/video23"     # USB UVC 摄像头 (GPS口的XHCI高速口)
WIDTH = 640                 # 采集分辨率宽
HEIGHT = 480                # 采集分辨率高
FPS = 15                    # 采集帧率 (摄像头支持60fps, 15fps足够)
FORMAT = "mjpeg"            # 像素格式: mjpeg (压缩) / yuyv (原始)

# ============================================================
# 帧缓存
# ============================================================
FRAME_CACHE_PATH = "/tmp/camera_frame.jpg"   # 降级兜底: 磁盘帧缓存
FRAME_QUEUE_SIZE = 3                         # 内存环形队列长度 (溢出丢旧帧)

# ============================================================
# v4l2-ctl 流式采集 (板子预装 v4l2-ctl, 不依赖 ffmpeg)
# ============================================================
FFMPEG_RETRY_MAX = 3        # 采集进程连续重启失败上限 (超限→离线)
FFMPEG_RETRY_DELAY = 1.0    # 重启间隔 (秒)
FFMPEG_CAPTURE_TIMEOUT = 3  # v4l2-ctl 启动超时 (秒)

# ============================================================
# AI 预警参数
# ============================================================
ALERT_DEBOUNCE_FRAMES = 2   # 预警防抖帧数 (连续N帧同向变化才生效)
SPEED_WARN_THRESHOLD = 0.01  # 接近速度: 警告阈值 (面积增长比率)
SPEED_DANGER_THRESHOLD = 0.03  # 接近速度: 危险阈值
NPU_INFER_TIMEOUT = 5.0      # 单帧 NPU 推理超时 (秒)
AI_VOICE_COOLDOWN = 10.0     # 语音播报冷却 (秒, 同等级不重复播)

# ============================================================
# 热插拔监控
# ============================================================
HOTPLUG_SCAN_INTERVAL = 5.0  # 设备扫描间隔 (秒, 0=禁用)
